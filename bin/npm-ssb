#!/usr/bin/env node
// jshint esversion: 6
process.title = "npm-ssb";
const registry = require('../registry');
const backend = require('../backend');

registry(backend, (err)=>{
    if (err) throw err;
});
